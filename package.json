{
  "name": "ibpm-remote",
  "version": "0.5.0",
  "main": "index.js",
  "author": "https://github.com/sammich",
  "license": "MIT",
  "scripts": {
    "test": "node --inspect ./node_modules/jest/bin/jest.js --runInBand --silent=false --colors --verbose --watch",
    "e2e": "node node_modules/.bin/jest --config jest.integration.conf.json --silent=false --colors --verbose"
  },
  "readmeFilename": "README.md",
  "devDependencies": {
    "dotenv": "^6.0.0",
    "jest": "^23.2.0",
    "markdown-toc": "^1.2.0",
    "query-string": "^6.1.0",
    "request": "^2.87.0"
  },
  "jest": {
    "testRegex": "(/__tests__/.*|\\.(test))\\.js$"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "dependencies": {
    "moment": "^2.22.2"
  }
}
